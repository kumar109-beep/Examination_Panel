export function datePicker(label) {
    var datepicker = '<label>' + label +'</label>\
                    <input type="text" class="form-control datepicker hasDatepicker" placeholder = "dd/MM/yyyy" value = "" />';
    return datepicker;
}
